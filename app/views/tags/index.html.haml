%h1 Search
= form_tag(search_path, method: :get) do
  = text_field_tag :term, params[:term]
  = submit_tag 'Поиск', name: nil
#photos
  - @tags.each do |tag|
    .tag.photo
      = link_to(image_tag(tag.photo.image.thumb.url, alt: 'Фотография'), tag.photo, target: '_blank') if tag.photo.image?
